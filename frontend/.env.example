# Use your workstation's LAN IP for Expo Go on a physical device.
# The current app communicates only with the backend API; never put service-role secrets in Expo variables.
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:5000/api
